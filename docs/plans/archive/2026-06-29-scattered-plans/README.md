# Archived Scattered Plans (2026-06-29)

These files are **historical** planning artifacts consolidated out of the repo
root, `docs/`, `backend/`, `subagent-artifacts/`, and `.task-reports/` on
2026-06-29. They are preserved here for traceability only.

## ⚠️ May be stale / superseded

- Do **not** treat anything here as the current plan.
- Contents may be outdated, partially implemented, or entirely superseded.
- File names are flattened (`/` → `-`) and prefixed with their original location
  so the origin of each file is traceable.
- The four originally git-tracked files were moved with `git mv` (history
  preserved): `HANDOFF.md`, `docs--model-library-download-profile-plan.md`,
  `docs--ltx-offline-research--04-recommended-implementation-plan.md`,
  `docs--ltx-offline-research--05-ltx-desktop-model-profiles-and-gguf-kijai-plan.md`.
  The rest were untracked local artifacts.
- **Traceability caveat:** the prior detailed root `plan.md` body is **not**
  duplicated in this archive folder — it was rewritten in place to a short
  pointer (see root [`plan.md`](../../../../plan.md)). Its earlier detailed
  content remains reachable via `git log -p -- plan.md` / `git show` history,
  not via a file here. So "preserved for traceability" applies to the archived
  files listed above, not to the old root `plan.md` body.

## Current source of truth

The canonical, current roadmap and subplans live at:

→ [`../../current/README.md`](../../current/README.md)

If a topic here conflicts with the current plan, the **current plan wins**.
